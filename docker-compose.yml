version: '3.5'

services:
    base_container:
        build:
            args:
                FROM_BASE: alpine:3.6
                CONTAINER_VERSION: 1.0.0
            cache_from:
                - alpine:3.6
            context: .
        image: base_container:20180210
