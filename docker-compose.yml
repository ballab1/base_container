version: '3.5'

services:
    base_container:
        build:
            args:
                FROM_BASE: alpine:3.6
                CONTAINER_VERSION: 1.0.2
                TZ: 'America/New_York'
            cache_from:
                - alpine:3.6
            context: .
        image: base_container:20180217
